1,FebsSecurityConfigure(继承WebSecurityConfigurerAdapter)用于处理/oauth开头的请求，Spring Cloud OAuth内部定义的获取令牌，
  刷新令牌的请求地址都是以/oauth/开头的，也就是说FebsSecurityConfigure用于处理和令牌相关的请求。
2,FebsResourceServerConfigure(继承ResourceServerConfigurerAdapter)用于处理非/oauth/开头的请求，其主要用于资源的保护，
  客户端只能通过OAuth2协议发放的令牌来从资源服务器中获取受保护的资源。
这是因为当我们引入了spring-cloud-starter-oauth2依赖后，系统会暴露一组由/oauth开头的端点

FebsUserDetailService是密码模式登陆
RedisClientDetailsService是授权码模式

一，redis启动
1，进入E:\Redis-x64-3.0.504
2，当前目录 cmd
  redis-server.exe redis.windows.conf

二，Nacos启动
1，进入E:\nacos\bin
2，双击bin/startup.cmd
3，访问http://localhost:8001/nacos
   用户名febs密码123456

三，启动微服务

FebsTxManagerApplication;
FebsAdminApplication;
FebsAuthApplication;
FebsServerSystemApplication;
FebsGatewayApplication。
